{
    "name": "Tulisp CI",
    "on": [ "push" ],
    "jobs": {
        "build": {
            "runs-on": "ubuntu-latest",
            "steps": [
                { "uses": "actions/checkout@v1" },
                {
                    "name": "Install Nix",
                    "run": "nix/install --daemon"
                },
                {
                    "name": "Build Tulisp",
                    "run": "/nix/var/nix/profiles/default/bin/nix run -ic tulisp/cargo build"
                }
            ]
        }
    }
}
